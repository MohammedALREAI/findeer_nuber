export enum TargetType {
  Email = "EMAIL",
  Phone = "PHONE",
}

export enum TargetType {
  Email = "EMAIL",
  Phone = "PHONE",
}

export enum StatusState {
  ACCEPTED = "ACCEPTED",
  FINISHED = "FINISHED",
  CANCELED = "CANCELED",
  REQUESTING = "REQUESTING",
  ONROUTE = "ONROUTE",
}
export type rideStatus =
  | "ACCEPTED"
  | "FINISHED"
  | "CANCELED"
  | "REQUESTING"
  | "ONROUTE";
